# MarketPulse AI Scripts

This folder contains utility scripts for running and managing the application.

## Files:
- `run.py` - Start the FastAPI backend server
- `setup.py` - Package setup and installation script

## Usage:
- Start API server: `python scripts/run.py`
- Install package: `python scripts/setup.py install`