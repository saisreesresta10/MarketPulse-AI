# MarketPulse AI - Railway Deployment Requirements
# Optimized for Railway's free tier

# Core web framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6

# Streamlit UI
streamlit>=1.28.0
plotly>=5.17.0

# Data processing (lightweight versions)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Database (SQLite only for free tier)
sqlalchemy>=2.0.0
alembic>=1.13.0

# Security (minimal)
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0

# Data validation
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0

# HTTP requests
requests>=2.31.0
httpx>=0.25.0

# Utilities
python-dateutil>=2.8.2
python-dotenv>=1.0.0
click>=8.1.0

# Testing (optional)
pytest>=7.4.0
hypothesis>=6.90.0